# ✂️ Code Snippet Manager


A full-stack MERN application to store, manage, and retrieve code snippets efficiently. Built as part of the "15 Days, 15 Projects" challenge.

## 🚀 Tech Stack

**Frontend:**
* React (Vite)
* Lucide React (Icons)
* Axios (API Requests)
* Custom CSS (Dark Mode Editor Theme)

**Backend:**
* Node.js & Express
* MongoDB & Mongoose
* **Zod** (Data Validation)
* Cors (Cross-Origin Resource Sharing)

---

## ✨ Features

* **Create Snippets:** Save code with titles and specific languages (JS, Python, SQL, etc.).
* **Data Validation:** Backend uses `Zod` to ensure no invalid data reaches the database.
* **One-Click Copy:** Copy code blocks instantly to the clipboard.
* **Recent First:** Automatically sorts snippets by the newest created.
* **Syntax UI:** Dark-mode code block styling for better readability.

---

## 🛠️ Installation & Setup

### 1. Prerequisites
* Node.js installed.
* MongoDB installed and running locally (or use MongoDB Atlas).

### 2. Clone the Repository
```bash
git clone <your-repo-url>
cd snippet-manager
